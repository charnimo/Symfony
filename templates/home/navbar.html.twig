<link rel="stylesheet" href="{{ asset('styles/navbar.css') }}">
<nav class="navbar navbar-expand-lg navbar-light fixed-top">
    <div class="container">
        <a class="navbar-brand" href="#">SHOP</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item active">

                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('user_products') }}">Products</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_contact') }}">Contact</a>
                </li>
                {% if not app.session.get('admin') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('index') }}">Cart</a>
                    </li>
                {% endif %}

                {% if app.session.get('admin') or app.session.get('user') %}
                    <li class="nav-item">
                        <a class="nav-link" href="/logout.php">Logout</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">Login</a>
                    </li>
                {% endif %}
                <div id="user-status" style="margin-left: 100px;">
                    {% if app.session.get('admin') %}
                        <li class="nav-item"><a class="nav-link">Logged in as: Admin</a></li>
                    {% elseif app.session.get('user') %}
                        <li class="nav-item"><a class="nav-link">Logged in as: {{ app.session.get('user') }}</a></li>
                    {% else %}
                        <li class="nav-item"><a class="nav-link">Not logged in</a></li>
                    {% endif %}
                </div>
            </ul>
        </div>
    </div>
</nav>
